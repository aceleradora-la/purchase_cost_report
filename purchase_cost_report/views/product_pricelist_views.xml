<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Agrega el campo "Nota de Valuación" al form de ítems de lista de precios -->
    <record id="product_pricelist_item_form_cost_note" model="ir.ui.view">
        <field name="name">product.pricelist.item.form.purchase.cost.note</field>
        <field name="model">product.pricelist.item</field>
        <field name="inherit_id" ref="product.product_pricelist_item_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='fixed_price']" position="after">
                <div invisible="not purchase_cost_note"
                     style="margin-top: 8px; padding: 6px 10px; background: #f8f9fa; border-left: 3px solid #6c757d; border-radius: 3px; font-size: 12px; color: #555; white-space: pre-wrap;">
                    <field name="purchase_cost_note" readonly="1" nolabel="1" widget="text"/>
                </div>
            </xpath>
        </field>
    </record>

</odoo>
