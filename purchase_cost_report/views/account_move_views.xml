<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="account_move_form_cost_report_button" model="ir.ui.view">
        <field name="name">account.move.form.purchase.cost.report</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <!-- Campo invisible ANTES del button_box para que el frontend lo cargue -->
            <xpath expr="//div[@name='button_box']" position="before">
                <field name="purchase_cost_show_button" invisible="1"/>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_print_purchase_cost_report"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-file-text-o"
                    string="Valuación OC"
                    invisible="not purchase_cost_show_button"
                    title="Reporte de valuación en moneda de compra"
                />
            </xpath>
        </field>
    </record>

</odoo>
